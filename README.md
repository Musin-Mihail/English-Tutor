# English Tutor AI

**English Tutor AI** — это веб-приложение для практики перевода с русского на английский язык с использованием искусственного интеллекта. Система выступает в роли репетитора: дает задания, проверяет переводы, объясняет грамматические ошибки, ведет журнал обучения и отслеживает успеваемость по конкретным темам.

Особенностью проекта является **хранение прогресса в Markdown-файлах**, что позволяет легко читать и редактировать историю обучения вручную.

## ✨ Основные возможности

- **Генерация заданий:** AI анализирует вашу успеваемость и генерирует новые предложения для перевода, делая упор на темы, в которых у вас пробелы.
- **Умная проверка:** Использование модели Google Gemini (Gemma) для проверки перевода, поиска ошибок и оценки от 0 до 10.
- **Детальный фидбек:** Объяснение ошибок, правильные варианты перевода, альтернативы и рекомендации.
- **Прозрачная история:** Весь прогресс (задания, ответы, разборы ошибок) сохраняется в читаемом файле `ENGLISH_training_journal.md`.
- **Таблица успеваемости:** Автоматический пересчет среднего балла по грамматическим темам (Артикли, Времена, Предлоги и т.д.) в файле `ENGLISH_performance_table.md`.
- **Современный интерфейс:** Фронтенд на Angular с использованием Material Design в формате чата.

## 🛠 Технический стек

### Backend (Серверная часть)

- **Python**
- **FastAPI** — веб-фреймворк.
- **Google Generative AI** — API для доступа к LLM моделям (Gemma/Gemini).
- **Pydantic** — валидация данных.
- **Файловая система** — используется как база данных (Markdown).

### Frontend (Клиентская часть)

- **Angular 21** — основной фреймворк.
- **Angular Material** — компоненты UI.
- **SCSS** — стилизация.
- **Signals** — управление состоянием.

## 🚀 Установка и запуск

Для работы проекта требуются установленные **Python** и **Node.js**.

### 1. Настройка Бэкенда

1. Перейдите в папку `backend`:

```bash
cd backend
```

2. Создайте виртуальное окружение (рекомендуется):

```bash
python -m venv venv
# Windows:
venv\Scripts\activate
# Mac/Linux:
source venv/bin/activate
```

3. Установите зависимости:

```bash
pip install -r requirements.txt
```

4. **Настройка окружения (.env):**
   В папке `backend` переименуйте файл-пример `.env.example` в `.env`:

- Откройте `.env` и вставьте ваш ключ `GOOGLE_API_KEY`.
- **Прокси:** Если вы находитесь в регионе, где Google API заблокирован (например, РФ), раскомментируйте и настройте переменную `PROXY_URL` (например, `http://127.0.0.1:12334`). Код в `run.py` автоматически подхватит эти настройки.

5. Запустите сервер:

```bash
python run.py
```

Сервер запустится на `http://0.0.0.0:8000`.

> **Примечание:** При первом запуске приложение **автоматически создаст** файлы журнала и таблицы успеваемости из шаблонов в папке `app/data/`.

### 2. Настройка Фронтенда

1. Перейдите в папку `frontend`:

```bash
cd frontend
```

2. Установите зависимости (используя npm):

```bash
npm install
```

3. Запустите приложение:

```bash
ng serve
```

Или:

```bash
npm start
```

Приложение будет доступно по адресу `http://localhost:4200`.

## 📂 Структура данных

Прогресс студента хранится в папке `backend/app/data/`.
_Эти файлы игнорируются git'ом, чтобы сохранить вашу приватность. При первом старте они создаются из шаблонов._

- **`ENGLISH_training_journal.md`**: Журнал заданий. Содержит историю всех диалогов с учителем, включая ваши ответы и исправления.
- **`ENGLISH_performance_table.md`**: Таблица навыков. Содержит текущий уровень, список изученных слов и средние баллы по темам (например, "Времена Past", "Артикли").

## 🖥 Использование

1. Откройте браузер на `http://localhost:4200`.
2. AI-учитель предложит вам задание на русском языке (например: _"Вчера я ходил в кино"_).
3. Введите перевод в поле ввода и нажмите `Ctrl+Enter` или кнопку отправки.
4. Дождитесь проверки. Вы увидите:

- Вашу оценку (x/10).
- Правильный вариант.
- Список ошибок с объяснениями.
- Рекомендации.

5. Нажмите "Send" еще раз (или подождите), чтобы получить следующее задание, сформированное на основе ваших ошибок.

---

**Автор:** Проект создан с использованием связки Angular + FastAPI + Google Gemini.
